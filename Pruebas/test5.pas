FUN main( arg:INT, arg2:INT[189] )
x:INT;
BEGIN
	PRINT( "Una cadena de text" );
	PRINT( "1 Una cadena de text" );	
	WRITE( 5 + 2 / 2 );
	x := 567;
	IF 3 > x THEN
		PRINT ("X")
	ELSE
		READ( mivar[5] );
	RETURN x
END